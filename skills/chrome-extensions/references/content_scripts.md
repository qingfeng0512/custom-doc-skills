# Chrome-Extensions - Content Scripts

**Pages:** 3

---

## 匹配模式 使用集合让一切井井有条 根据您的偏好保存内容并对其进行分类。

**URL:** https://developer.chrome.google.cn/docs/extensions/develop/concepts/match-patterns?hl=zh-cn

**Contents:**
- 匹配模式 使用集合让一切井井有条 根据您的偏好保存内容并对其进行分类。
- 特殊情况
- 示例格式

匹配模式是一种具有以下结构的网址，用于指定一组网址：

scheme：必须是以下各项之一，并使用英文冒号后跟双斜线 (://) 与模式的其余部分分隔：

如需了解如何将内容脚本注入不受支持的架构（例如 about: 和 data:），请参阅在相关帧中注入。

host：主机名 (www.example.com)。主机名前面的 * 用于匹配子网域 (*.example.com)，或者仅使用通配符 *。 - 如果您在主机模式中使用通配符，则通配符必须是第一个字符或唯一字符，并且必须紧跟英文句点 (.) 或正斜线 (/)。

path：网址路径 (/example)。对于主机权限，路径是必需的，但会被忽略。应遵循惯例使用通配符 (/*)。

**Examples:**

Example 1 (unknown):
```unknown
<scheme>://<host>/<path>
```

---

## 匹配模式 使用集合让一切井井有条 根据您的偏好保存内容并对其进行分类。

**URL:** https://developer.chrome.google.cn/docs/extensions/develop/concepts/match-patterns

**Contents:**
- 匹配模式 使用集合让一切井井有条 根据您的偏好保存内容并对其进行分类。
- 特殊情况
- 示例格式

匹配模式是一种具有以下结构的网址，用于指定一组网址：

scheme：必须是以下各项之一，并使用英文冒号后跟双斜线 (://) 与模式的其余部分分隔：

如需了解如何将内容脚本注入不受支持的架构（例如 about: 和 data:），请参阅在相关帧中注入。

host：主机名 (www.example.com)。主机名前面的 * 用于匹配子网域 (*.example.com)，或者仅使用通配符 *。 - 如果您在主机模式中使用通配符，则通配符必须是第一个字符或唯一字符，并且必须紧跟英文句点 (.) 或正斜线 (/)。

path：网址路径 (/example)。对于主机权限，路径是必需的，但会被忽略。应遵循惯例使用通配符 (/*)。

**Examples:**

Example 1 (unknown):
```unknown
<scheme>://<host>/<path>
```

---

## 匹配模式 使用集合让一切井井有条 根据您的偏好保存内容并对其进行分类。

**URL:** https://developer.chrome.google.cn/docs/extensions/match_patterns?hl=zh-cn

**Contents:**
- 匹配模式 使用集合让一切井井有条 根据您的偏好保存内容并对其进行分类。
- 特殊情况
- 示例格式

匹配模式是一种具有以下结构的网址，用于指定一组网址：

scheme：必须是以下各项之一，并使用英文冒号后跟双斜线 (://) 与模式的其余部分分隔：

如需了解如何将内容脚本注入不受支持的架构（例如 about: 和 data:），请参阅在相关帧中注入。

host：主机名 (www.example.com)。主机名前面的 * 用于匹配子网域 (*.example.com)，或者仅使用通配符 *。 - 如果您在主机模式中使用通配符，则通配符必须是第一个字符或唯一字符，并且必须紧跟英文句点 (.) 或正斜线 (/)。

path：网址路径 (/example)。对于主机权限，路径是必需的，但会被忽略。应遵循惯例使用通配符 (/*)。

**Examples:**

Example 1 (unknown):
```unknown
<scheme>://<host>/<path>
```

---
